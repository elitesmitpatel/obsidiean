# Firebase Hosting Integration

Production site: `notes-oo.web.app`.

SPA rewrite: `** → /index.html` supports React Router deep links.

Deployment depends on `firebase.json` and `.firebaserc`.

Change Impact: Router changes and Hosting rewrite changes can break direct URL navigation.
