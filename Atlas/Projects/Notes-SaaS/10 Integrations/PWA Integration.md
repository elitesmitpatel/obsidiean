# PWA Integration

Build generates manifest and service worker through the Vite/PWA setup.

App shell assets are precached; note content is not.

Installability was verified in production.

Change Impact: build configuration, manifest, service worker, hosting and offline expectations.
