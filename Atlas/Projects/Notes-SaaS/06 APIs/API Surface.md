# API Surface

This project does not expose a custom HTTP backend. The application consumes SDK-level APIs:

- [[Firebase Authentication API]]
- [[Firestore API]]
- [[IndexedDB API]]

Firebase Hosting serves the built SPA but is not an application CRUD API.

## Data access functions

See [[Service API Catalog]].
