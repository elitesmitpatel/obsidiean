# Offline Service API

Source: `src/features/offline/services/offlineService.ts`.

- `calculateContentSize(content)`
- `downloadNote(note)`
- `removeDownload(noteId)`
- `getDownloadedNote(noteId)`
- `getAllDownloadedNotes()`
- `isNoteDownloaded(noteId)`
- `getTotalStorageUsed()`
- `updateDownloadedNote(note)`

All persistence targets [[Downloaded Notes Table]].
