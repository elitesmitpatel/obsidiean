# Admin Subjects Screen

Route: `/admin/subjects`.

Guard: [[AdminRoute]].

Lists all subjects and opens create/edit forms.

Change Impact: subject admin service, subject schema, indexes, admin tests.
