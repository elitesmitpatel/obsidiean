# Admin Note Form

Routes: `/admin/notes/new`, `/admin/notes/:noteId/edit`.

Creates/updates Markdown note content, tags, order, status and relationships.

`searchableText` is derived; there is no attachment/file input.

Change Impact: note schema, versioning, search, offline behavior, admin service.
