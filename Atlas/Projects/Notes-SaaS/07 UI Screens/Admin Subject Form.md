# Admin Subject Form

Routes: `/admin/subjects/new`, `/admin/subjects/:subjectId/edit`.

Creates/updates subject fields and active state.

Change Impact: subject schema/service/rules and admin list invalidation.
