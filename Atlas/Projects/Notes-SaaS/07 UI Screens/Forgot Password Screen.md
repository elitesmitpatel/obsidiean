# Forgot Password Screen

Route: `/forgot-password`.

Guard: [[PublicOnlyRoute]].

Sends password-reset email through Firebase Auth.

Change Impact: Firebase Auth configuration, auth service, reset tests.
