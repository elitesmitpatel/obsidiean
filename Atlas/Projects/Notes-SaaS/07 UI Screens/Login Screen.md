# Login Screen

Route: `/login`.

Guard: [[PublicOnlyRoute]].

Uses [[Auth Module]] to authenticate email/password.

Links to `/signup` and `/forgot-password`.

Change Impact: [[Authentication]], [[Router]], auth error mapping, login tests.
